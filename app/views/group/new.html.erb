<h1>Group#new</h1>
<p>Find me in app/views/group/new.html.erb</p>

<%= form_with scope: :group, url: group_index_path, local: true do |form| %>
  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@group.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @group.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= form.label :groupname %><br>
    <%= form.text_area :groupname %>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %>

<%= link_to "Back" , group_index_path %>
